import 'package:flutter/material.dart';

class ProcuctCreatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('data'),
    );
  }
}
